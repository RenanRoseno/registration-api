package com.queonetics.registration.models.dto;

public record RecordConductorDTO(Long id,
                                 String name,
                                 String registration) {
    // Não precisa das anotações do lombok
}
